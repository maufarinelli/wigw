(function(angular, exponentMock) {
    'use strict';

    describe('Exponent Service', function() {
        var ExponentComponent;

        beforeEach(module('exponent'));
        beforeEach(inject(function(_ExponentComponent_) {
            ExponentComponent = _ExponentComponent_;
        }));

        it('it should be a class containing ', function() {

        });


    });

})(window.angular, window.exponentMock);